# Examples

This folder contains a small toy example for testing the ECHO pipeline.

The example is not used for full experimental evaluation. It is only provided to help users understand the input format and quickly check whether the code can run.